# 旅行助手命令行服务

这是一个基于状态机的旅行助手命令行服务，让你可以通过命令行与 AI 助手进行对话，规划旅行。

## 功能特点

- 🤖 **智能对话**: 基于大模型的自然语言交互
- 📊 **状态管理**: 完整的状态机管理对话流程
- 💾 **会话持久化**: 自动保存和加载对话会话
- 📋 **信息收集**: 自动收集目的地、预算、日期、用户画像等信息
- 🔄 **上下文感知**: 根据已收集信息生成个性化回复

## 安装和运行

### 1. 环境准备

确保已安装所有依赖项：

```bash
pip install -r requirements.txt
```

### 2. 启动服务

```bash
python run_cli.py
```

## 使用方法

### 基本命令

| 命令 | 说明 | 示例 |
|------|------|------|
| `/new` | 创建新会话 | `/new` |
| `/load <id>` | 加载现有会话 | `/load 123` |
| `/save` | 保存当前会话 | `/save` |
| `/info` | 显示会话信息 | `/info` |
| `/status` | 显示当前状态 | `/status` |
| `/slots` | 显示已收集的槽位信息 | `/slots` |
| `/history` | 显示对话历史 | `/history` |
| `/help` | 显示帮助信息 | `/help` |
| `/quit` | 退出程序 | `/quit` |

### 对话流程

1. **启动新会话**
   ```
   /new
   ```

2. **回答助手问题**
   ```
   💬 请输入消息或命令: 我想去成都旅游
   ```

3. **继续对话**
   ```
   💬 请输入消息或命令: 预算大概5000元
   ```

4. **查看状态**
   ```
   /status
   ```

5. **查看已收集信息**
   ```
   /slots
   ```

## 对话示例

```
🚀 启动旅行助手命令行服务...
输入 /help 查看帮助信息

💬 请输入消息或命令: /new

🎉 新会话已创建 (ID: 1)
==================================================
欢迎使用旅行助手！我将帮助您规划完美的旅行。
==================================================
🤖 您好！我很高兴为您规划旅行。请问您这次想去哪个城市或景区呢？

💬 请输入消息或命令: 我想去成都旅游

🤖 太好了！成都是一个非常棒的选择。现在请告诉我您的预算大概是多少呢？

📊 当前状态: SLOT_FILLING_BUDGET
   阶段: slot_filling
   意图: fill_budget
   上下文: 等待用户提供预算信息

💬 请输入消息或命令: 预算5000元

🤖 好的，5000元的预算很合理。接下来请告诉我您计划什么时候出发、什么时候回来呢？

📊 当前状态: SLOT_FILLING_DATES
   阶段: slot_filling
   意图: fill_dates
   上下文: 等待用户提供出发和返回日期

💬 请输入消息或命令: /slots

📋 槽位信息:
==============================
destination : ✅ 已填充
             值: 成都
budget       : ✅ 已填充
             值: 5000元
dates        : ❌ 未填充
profile      : ❌ 未填充
==============================
```

## 状态机流程

状态机包含以下状态：

1. **INIT** - 初始状态
2. **SLOT_FILLING_DESTINATION** - 收集目的地
3. **SLOT_FILLING_BUDGET** - 收集预算
4. **SLOT_FILLING_DATES** - 收集日期
5. **SLOT_FILLING_PROFILE** - 收集用户画像
6. **CONFIRMATION** - 确认信息
7. **COMPLETED** - 完成状态
8. **ERROR** - 错误状态

## 技术架构

- **状态机**: 使用 `transitions` 库管理对话状态
- **大模型**: 集成蓝心大模型 API 生成自然回复
- **数据存储**: 使用 Django ORM 和 SQLite 数据库
- **会话管理**: 自动保存和恢复对话状态

## 故障排除

### 常见问题

1. **导入错误**
   - 确保已安装所有依赖项
   - 检查 Django 设置是否正确

2. **数据库错误**
   - 运行 `python manage.py migrate` 创建数据库表
   - 检查数据库连接配置

3. **API 错误**
   - 检查蓝心大模型 API 配置
   - 确保 `VIVO_APP_ID` 和 `VIVO_APP_KEY` 已正确设置

### 日志查看

服务运行时会输出详细的日志信息，包括：
- 状态机状态变化
- API 调用结果
- 错误信息

## 开发说明

### 文件结构

```
talker/services/
├── cli_service.py      # 命令行服务主文件
├── state_machine.py    # 状态机实现
├── chat_service.py     # 大模型服务
└── session_control.py  # 会话控制服务
```

### 扩展功能

可以通过以下方式扩展功能：

1. **添加新的状态**: 在 `state_machine.py` 中定义新状态和转移
2. **自定义回复**: 在 `prompts.yaml` 中添加新的 prompt 模板
3. **增强命令**: 在 `cli_service.py` 中添加新的命令处理

## 许可证

本项目采用 MIT 许可证。 