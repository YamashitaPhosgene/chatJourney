---
description: 
globs: 
alwaysApply: true
---

### 不要主动生成测试或文档

- **强制**：除非用户在同一次消息中明确请求，否则不得创建、补全或修改任何形式的测试代码  
  （包括但不限于 `*.test.*` / `*.spec.*`、`__tests__` 目录、e2e 脚本、Mock 等）。

- **强制**：同样，除非用户明确要求，否则不得生成或更新任何项目文档  
  （README、CHANGELOG、API 说明、JSDoc/Sphinx 注释块、Swagger/OpenAPI 描述等）。

- 如果实现功能导致已有测试/文档报错或失效：  
  1. 先提醒用户问题所在；  
  2. 仅在用户同意后，再生成或修改相关测试/文档。

- 遵循最小改动原则：若模板代码（如 CRA、NestJS 脚手架）自带示例测试或说明文档，也应在生成时一并移除，确保本规则不被绕过。


- 一旦用户之后**显式**要求生成测试或文档，本规则不再限制此次请求，正常输出所需内容。